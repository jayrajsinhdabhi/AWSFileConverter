{
  "name": "aws-file-converter-backend",
  "version": "1.0.0",
  "description": "Lambda backend for AWS File Converter",
  "main": "src/handler.js",
  "scripts": {
    "test": "jest --testPathPattern='tests/' --coverage",
    "lint": "eslint src/ tests/"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "busboy": "^1.6.0",
    "pdfkit": "^0.15.0",
    "pdf-parse": "^1.1.1",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
